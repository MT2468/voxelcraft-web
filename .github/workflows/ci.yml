name: VoxelCraft CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: JavaScript syntax
        run: node --check src/game.js
      - name: Required project files
        shell: bash
        run: |
          test -s index.html
          test -s style.css
          test -s src/game.js
          grep -q 'type="module" src="./src/game.js"' index.html
          grep -q 'three@0.185.1' index.html
          grep -q 'PointerLockControls' src/game.js
          grep -q 'rebuildAllChunks' src/game.js
          grep -q 'localStorage' src/game.js
